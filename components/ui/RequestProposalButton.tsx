const RequestProposalButton = () => {
  return (
    <button
      type="button"
      className="rounded-lg bg-primary px-4 py-2 text-sm font-medium text-on-primary shadow-sm transition hover:bg-primary/90 focus:outline-none focus-visible:ring-2 focus-visible:ring-primary-focus"
    >
      Request Proposal
    </button>
  )
}

export default RequestProposalButton
